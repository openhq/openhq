<h1><%= t("projects") %> </h1>

<div class="ui-projects-list pure-g">
  <% if @projects.any? %>
    <%= render @projects %>
  <% end %>
  <%= render "new_project" %>
</div>

<%= link_to "View Archived", archived_projects_path %>