<h1><%= t("edit_project") %></h1>

<%= render 'form' %>

<% if can? :destroy, @story %>
  <%= link_to @project, class: "icon-link link-large danger", method: :delete, data: { confirm: "Are you sure?" } do %>
    <span class="icon icon-trash"></span>
    Archive this project
  <% end %>
<% end %>