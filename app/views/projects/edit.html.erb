<h1><%= t("edit_project") %></h1>

<%= render 'form' %>

<%= simple_form_for @project, method: :delete, html: {class: "pure-form pure-form-stacked"} do |f| %>
  <%= f.submit "Archive this project" %>
<% end %>